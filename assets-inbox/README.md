# assets-inbox

Drop raw photo/logo files here for processing (cropping, background
removal, optimization) into `src/images/` or `public/`. Contents are
gitignored — this is a scratch folder, not part of the built site.
